name: fluda
description: Fluda is a library which makes Flutter application development faster and easier. It makes your code clean and easy to read.
version: 1.0.0
homepage: https://fluda.simonit.dev

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  url_launcher: ^6.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

